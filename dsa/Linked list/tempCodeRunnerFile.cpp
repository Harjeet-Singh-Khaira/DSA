
    // cin>>key;
    // searchKey(head,key);
    // cout<<"Enter element to be deleted: ";
    // cin>>del;
    // deletion(head,del);
    // display(head);
    // cout<<endl<<"Reversed array: \n";
    // display(reverse(head));